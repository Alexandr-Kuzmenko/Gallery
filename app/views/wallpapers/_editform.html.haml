= form_for @wallpaper do |f|
  .row
    %table
      %tbody
        %tr
          %td
            = f.label :title, 'Image name:'
          %td
            = f.text_field :title
        %tr
          %td
            = f.label :category, 'Category: '
          %td
            = f.collection_select :category_id, @categories, :id, :name
  .actions
    = f.submit 'Save', class: 'btn btn-success'