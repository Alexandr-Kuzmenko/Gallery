<!DOCTYPE html>
<html>
  <head>
    <link href="app/assets/stylesheets/categories.scss" rel="stylesheet" type="text/css">  
  </head>
  <body>
    <div class="row">
      <div class="col-xs-12"><a class="btn btn-success" href="<%=new_category_path%>">Create new category</a></div>
    </div>
    <br>
    <div class="row">
      <% @categories.each do |category| %>
        <% if category.wallpapers.present? %>
          <div class="col-xs-2" >
          <%= link_to category.name, category_path(category), class: "btn btn-primary" %><br>
          <%= link_to image_tag(category.wallpapers.sample.image.thumb.url, alt: 'image') %>
          </div>
        <% end %>
      <% end %>
    </div>
  </body>
</html>