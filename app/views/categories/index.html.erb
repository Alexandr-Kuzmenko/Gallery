<!DOCTYPE html>
<html>
  <head>
    <link href="app/assets/stylesheets/categories.scss" rel="stylesheet" type="text/css">  
  </head>
  <body>
    <div class="row">
      <div class="col-xs-12"><a class="btn btn-default" href="<%=new_category_path%>">Create new category</a></div>
    </div>
    <div class="row">
      <% @categories.each do |category| %>
        <a class="btn btn-info"><%= category.name %></a>
        <br><%= link_to 'view category', category_path(category) %>
        <br><%= link_to 'Delete', category_path(category), method: :delete,
        data: { confirm: 'Are you sure?' } %><br>
      <% end %>
    </div>
  </body>
</html>